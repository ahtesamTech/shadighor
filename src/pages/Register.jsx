import React from 'react';
import RegisterSteper from '../components/RegisterSteper';

const Register = () => {
    return (
        <div>
            <RegisterSteper></RegisterSteper>
            
        </div>
    );
};

export default Register;